"use client";

import Link from "next/link";
import Image from "next/image";
import { motion } from "framer-motion";
import { Terminal, Github, Brain, Mic, Code2, Zap, ChevronRight, Layers, GitBranch, Search } from "lucide-react";

const fadeUp = {
  hidden: { opacity: 0, y: 30 },
  show: { opacity: 1, y: 0, transition: { duration: 0.6 } },
};

const stagger = {
  hidden: {},
  show: { transition: { staggerChildren: 0.12 } },
};

const features = [
  {
    icon: <Mic className="w-6 h-6" />,
    title: "Live Avatar Conversations",
    desc: "Talk to your AI teammate Anna via a photorealistic Tavus avatar. Real voice, real face, real code help.",
    color: "from-amber-500/20 to-amber-600/5",
    border: "border-amber-500/20",
  },


  {
    icon: <Brain className="w-6 h-6" />,
    title: "Persistent Memory (Moorcheh)",
    desc: "Every conversation, file, and decision is semantically stored. Anna knows your codebase better than anyone.",
    color: "from-orange-500/20 to-orange-600/5",
    border: "border-orange-500/20",
  },
  {
    icon: <Terminal className="w-6 h-6" />,
    title: "Autonomous Execution (NanoClaw)",
    desc: "Anna doesn't just talk — she runs bash commands, reads files, commits code, and executes actions autonomously.",
    color: "from-green-500/20 to-green-600/5",
    border: "border-green-500/20",
  },
  {
    icon: <Github className="w-6 h-6" />,
    title: "GitHub Codebase Scout",
    desc: "Paste any GitHub URL. Anna instantly maps file trees, branches, commits, and PRs — then starts answering questions.",
    color: "from-blue-500/20 to-blue-600/5",
    border: "border-blue-500/20",
  },
  {
    icon: <GitBranch className="w-6 h-6" />,
    title: "5th Teammate Protocol",
    desc: "Anna understands your architecture, remembers your patterns, and suggests improvements like a senior dev would.",
    color: "from-amber-400/20 to-amber-500/5",
    border: "border-amber-400/20",
  },
  {
    icon: <Layers className="w-6 h-6" />,
    title: "Multi-Layer AI Stack",
    desc: "Powered by Gemini 2.5 Flash via Tavus, Moorcheh semantic memory, OpenAI via Puter, and NanoClaw execution.",
    color: "from-yellow-500/20 to-yellow-600/5",
    border: "border-yellow-500/20",
  },
];

const steps = [
  { num: "01", title: "Connect your Persona", body: "Anna is pre-built as a Tavus replica. No setup — she's ready to join your call the moment you load the app." },
  { num: "02", title: "Paste a GitHub URL", body: "Share any public repo. Revenant scouts its structure, history, branches, and PRs instantly via the GitHub API." },

  { num: "03", title: "Ask anything", body: "Voice or text — ask about architecture decisions, past bugs, contributor patterns, or have Anna write code live." },
  { num: "04", title: "Anna acts autonomously", body: "NanoClaw executes real commands. Moorcheh remembers everything. Your 5th teammate compounds intelligence over time." },
];

export default function LandingPage() {
  return (
  return (
    <div className="min-h-screen bg-[#0d0a05] text-white font-sans overflow-x-hidden">
      {/* BG Glows */}
      <div className="fixed inset-0 pointer-events-none">
        <div className="absolute top-[-20%] left-[-10%] w-[70vw] h-[70vw] bg-amber-600/10 rounded-full blur-[120px]" />
        <div className="absolute bottom-[-10%] right-[-10%] w-[50vw] h-[50vw] bg-orange-500/8 rounded-full blur-[120px]" />
      </div>


      {/* NAV */}
      <nav className="fixed top-0 left-0 right-0 z-50 flex items-center justify-between px-8 py-4 border-b border-white/5 backdrop-blur-xl bg-black/20">
        <div className="flex items-center gap-3">
          <Image src="/logo.png" alt="Revenent" width={36} height={36} className="rounded-xl" />
          <span className="text-lg font-bold tracking-wider">REVENENT</span>
        </div>
        <div className="hidden md:flex items-center gap-8 text-sm text-gray-400">
          <a href="#features" className="hover:text-white transition-colors">Features</a>
          <a href="#how-it-works" className="hover:text-white transition-colors">How it works</a>
          <a href="#stack" className="hover:text-white transition-colors">Tech Stack</a>
        </div>
        <Link
          href="/app"
          className="px-5 py-2 bg-amber-600 hover:bg-amber-500 text-white text-sm rounded-xl font-medium transition-all shadow-[0_0_20px_rgba(217,119,6,0.3)] hover:shadow-[0_0_30px_rgba(217,119,6,0.5)]"
        >
          Try it free →
        </Link>

      </nav>

      {/* HERO */}
      <section className="relative pt-40 pb-24 px-6 max-w-6xl mx-auto text-center">
        <motion.div initial="hidden" animate="show" variants={stagger}>
          <motion.div variants={fadeUp} className="inline-flex items-center gap-2 px-4 py-1.5 rounded-full border border-amber-500/30 bg-amber-500/10 text-amber-300 text-xs mb-8">
            <Zap className="w-3 h-3" />
            Powered by Tavus · Moorcheh · NanoClaw · Gemini 2.5
          </motion.div>

          <motion.h1 variants={fadeUp} className="text-6xl md:text-8xl font-black tracking-tight mb-6 leading-none">
            Your AI<br />
            <span className="bg-gradient-to-r from-amber-400 via-orange-400 to-yellow-500 bg-clip-text text-transparent">
              5th Teammate
            </span>
          </motion.h1>


          <motion.p variants={fadeUp} className="text-xl text-gray-400 max-w-2xl mx-auto mb-10 leading-relaxed">
            Revenent is a sentient developer clone that lives inside your codebase.
            She talks, codes, remembers, and executes — autonomously.
          </motion.p>

          <motion.div variants={fadeUp} className="flex items-center justify-center gap-4 flex-wrap">
            <Link
              href="/app"
              className="px-8 py-4 bg-amber-600 hover:bg-amber-500 text-white font-semibold rounded-2xl transition-all shadow-[0_0_30px_rgba(217,119,6,0.4)] hover:shadow-[0_0_50px_rgba(217,119,6,0.6)] text-lg"
            >
              Meet Anna →
            </Link>

            <a
              href="#how-it-works"
              className="px-8 py-4 bg-white/5 hover:bg-white/10 border border-white/10 text-white font-semibold rounded-2xl transition-all text-lg"
            >
              See how it works
            </a>
          </motion.div>
        </motion.div>

        {/* Hero UI Preview */}
        <motion.div
          initial={{ opacity: 0, y: 60, scale: 0.95 }}
          animate={{ opacity: 1, y: 0, scale: 1 }}
          transition={{ duration: 0.8, delay: 0.4 }}
          className="mt-20 relative mx-auto max-w-4xl"
        >
          <div className="rounded-3xl border border-white/10 bg-[#171108] overflow-hidden shadow-[0_40px_100px_rgba(234,179,8,0.1)]">

            <div className="flex items-center gap-2 px-5 py-3 border-b border-white/5">
              <div className="w-3 h-3 rounded-full bg-red-400/60" />
              <div className="w-3 h-3 rounded-full bg-yellow-400/60" />
              <div className="w-3 h-3 rounded-full bg-green-400/60" />
              <span className="ml-3 text-xs text-gray-500 font-mono">revenent.app — Anna's Clone Active</span>
            </div>
            <div className="relative bg-gradient-to-br from-[#1c1409] to-[#0d0a05] h-72 flex items-center justify-center">
              <div className="text-center">
                <div className="text-6xl mb-4">🤖</div>
                <p className="text-amber-300 font-medium">Anna is ready to code with you</p>
                <div className="mt-4 flex items-center justify-center gap-2">
                  <div className="w-2 h-2 rounded-full bg-green-400 animate-pulse" />
                  <span className="text-xs text-gray-400">Live avatar · NanoClaw active · Moorcheh synced</span>
                </div>
              </div>
            </div>

            <div className="px-4 py-3 flex gap-3 bg-black/40">
              <div className="flex-1 bg-white/5 rounded-xl px-4 py-2 text-sm text-gray-500">
                Ask Anna anything, or paste a GitHub URL to scout it...
              </div>
              <div className="w-9 h-9 bg-amber-600 rounded-xl flex items-center justify-center">
                <Mic className="w-4 h-4 text-white" />
              </div>
            </div>
          </div>
          {/* Glow under preview */}
          <div className="absolute -bottom-10 left-1/2 -translate-x-1/2 w-3/4 h-20 bg-amber-600/15 blur-3xl rounded-full" />
        </motion.div>

      </section>

      {/* FEATURES */}
      <section id="features" className="py-24 px-6 max-w-6xl mx-auto">
        <motion.div
          initial="hidden"
          whileInView="show"
          viewport={{ once: true }}
          variants={stagger}
          className="text-center mb-16"
        >
          <motion.p variants={fadeUp} className="text-amber-400 text-sm font-medium mb-3 uppercase tracking-widest">Capabilities</motion.p>
          <motion.h2 variants={fadeUp} className="text-4xl md:text-5xl font-black mb-4">Everything a teammate does,<br />done autonomously.</motion.h2>
          <motion.p variants={fadeUp} className="text-gray-400 text-lg max-w-xl mx-auto">Revenant combines four AI systems into one unified developer interface.</motion.p>
        </motion.div>


        <motion.div
          initial="hidden"
          whileInView="show"
          viewport={{ once: true }}
          variants={stagger}
          className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5"
        >
          {features.map((f) => (
            <motion.div
              key={f.title}
              variants={fadeUp}
              className={`group p-6 rounded-2xl border ${f.border} bg-gradient-to-br ${f.color} backdrop-blur-sm hover:border-white/20 transition-all cursor-default`}
            >
              <div className="text-white mb-4 opacity-80 group-hover:opacity-100 transition-opacity">{f.icon}</div>
              <h3 className="text-white font-bold mb-2">{f.title}</h3>
              <p className="text-gray-400 text-sm leading-relaxed">{f.desc}</p>
            </motion.div>
          ))}
        </motion.div>
      </section>

      {/* HOW IT WORKS */}
      <section id="how-it-works" className="py-24 px-6 max-w-5xl mx-auto">
        <motion.div
          initial="hidden"
          whileInView="show"
          viewport={{ once: true }}
          variants={stagger}
          className="text-center mb-16"
        >
          <motion.p variants={fadeUp} className="text-orange-400 text-sm font-medium mb-3 uppercase tracking-widest">How it works</motion.p>
          <motion.h2 variants={fadeUp} className="text-4xl md:text-5xl font-black">From zero to 5th teammate<br />in 60 seconds.</motion.h2>
        </motion.div>

        <motion.div
          initial="hidden"
          whileInView="show"
          viewport={{ once: true }}
          variants={stagger}
          className="grid grid-cols-1 md:grid-cols-2 gap-6"
        >
          {steps.map((s) => (
            <motion.div key={s.num} variants={fadeUp} className="flex gap-5 p-6 rounded-2xl border border-white/8 bg-white/3 hover:bg-white/5 transition-all">
              <div className="text-3xl font-black text-white/10 shrink-0 mt-1 font-mono">{s.num}</div>
              <div>
                <h3 className="text-white font-bold mb-2">{s.title}</h3>
                <p className="text-gray-400 text-sm leading-relaxed">{s.body}</p>
              </div>
            </motion.div>
          ))}
        </motion.div>
      </section>

      {/* TECH STACK */}
      <section id="stack" className="py-24 px-6 max-w-5xl mx-auto text-center">
        <motion.div initial="hidden" whileInView="show" viewport={{ once: true }} variants={stagger}>
          <motion.p variants={fadeUp} className="text-cyan-400 text-sm font-medium mb-3 uppercase tracking-widest">Built on</motion.p>
          <motion.h2 variants={fadeUp} className="text-4xl font-black mb-12">The most powerful AI stack<br />available today.</motion.h2>

          <motion.div variants={fadeUp} className="grid grid-cols-2 md:grid-cols-4 gap-4">
            {[
              { name: "Tavus", sub: "Photorealistic avatar", color: "text-amber-400" },
              { name: "Moorcheh", sub: "Semantic memory", color: "text-orange-400" },
              { name: "NanoClaw", sub: "Terminal execution", color: "text-green-400" },
              { name: "Gemini 2.5", sub: "Language model", color: "text-yellow-400" },
            ].map((t) => (

              <div key={t.name} className="p-5 rounded-2xl border border-white/8 bg-white/3 hover:bg-white/5 transition-all">
                <div className={`text-xl font-black mb-1 ${t.color}`}>{t.name}</div>
                <div className="text-xs text-gray-500">{t.sub}</div>
              </div>
            ))}
          </motion.div>
        </motion.div>
      </section>

      {/* CTA */}
      <section className="py-24 px-6 max-w-3xl mx-auto text-center">
        <motion.div initial="hidden" whileInView="show" viewport={{ once: true }} variants={stagger}>
          <motion.div variants={fadeUp} className="p-12 rounded-3xl border border-amber-500/10 bg-gradient-to-br from-amber-900/20 to-[#0d0a05] relative overflow-hidden">
            <div className="absolute inset-0 flex items-center justify-center">
              <div className="w-64 h-64 bg-amber-600/5 rounded-full blur-3xl" />
            </div>
            <div className="relative">
              <div className="text-6xl mb-6">🤖</div>
              <h2 className="text-4xl font-black mb-4">Ready to hire your 5th teammate?</h2>
              <p className="text-gray-400 mb-8">Anna is ready to join your next session. No setup. No waiting.</p>
              <Link
                href="/app"
                className="inline-block px-10 py-4 bg-amber-600 hover:bg-amber-500 text-white font-bold rounded-2xl transition-all shadow-[0_0_30px_rgba(217,119,6,0.4)] hover:shadow-[0_0_60px_rgba(217,119,6,0.6)] text-lg"
              >
                Start talking to Anna →
              </Link>
            </div>
          </motion.div>
        </motion.div>
      </section>


      {/* FOOTER */}
      <footer className="border-t border-white/5 px-8 py-8 flex items-center justify-between text-sm text-gray-600 max-w-6xl mx-auto">
        <div className="flex items-center gap-2">
          <Image src="/logo.png" alt="Revenent" width={24} height={24} className="rounded-lg opacity-60" />
          <span>Revenent © 2026</span>
        </div>
        <div className="flex gap-6">
          <Link href="/app" className="hover:text-gray-400 transition-colors">App</Link>
          <a href="https://github.com" className="hover:text-gray-400 transition-colors">GitHub</a>
        </div>
      </footer>
    </div>
  );
}
